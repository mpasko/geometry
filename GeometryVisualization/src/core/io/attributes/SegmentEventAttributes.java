package core.io.attributes;

public enum SegmentEventAttributes {
	FIGURE, ID, STYLE, AX, AY, BX, BY;

	final public static String symbol = "s";
	final public static int attributesNumber = values().length;
}
